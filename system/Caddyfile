# Caddyfile for reverse proxy config
{
	http_port 8000
}

:8000

# Reverse proxy to the API and rewrite the path
route /api* {
	uri strip_prefix /api
	reverse_proxy * app:8080
	# Cors
	header Access-Control-Allow-Origin *
}
