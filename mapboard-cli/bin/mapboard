#!/usr/bin/env bash

# Get the real path of the script
here=$(dirname $(realpath $0))
cli_root=$(dirname $here)

# Load the virtual environment
export PATH=$cli_root/.venv/bin:$PATH

# Run the script
mapboard "$@"
